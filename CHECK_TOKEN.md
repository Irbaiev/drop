# Как проверить, что токен постоянный для устройства

## Способ 1: Проверка в консоли браузера

1. Откройте консоль (F12)
2. Выполните:
```javascript
// Посмотреть текущий токен
localStorage.getItem('OFFLINE_USER_ACCESS_TOKEN')

// Посмотреть playerId
localStorage.getItem('OFFLINE_USER_PLAYER_ID')

// Декодировать токен и посмотреть playerId внутри
const token = localStorage.getItem('OFFLINE_USER_ACCESS_TOKEN');
if (token) {
  const payload = JSON.parse(atob(token.split('.')[1].replace(/-/g, '+').replace(/_/g, '/')));
  console.log('PlayerId в токене:', payload.session.playerId);
}
```

3. **Перезагрузите страницу** (F5)
4. Снова выполните команды - токен должен быть **таким же**

## Способ 2: Проверка в разных браузерах/устройствах

1. Откройте игру в **Chrome** - запишите токен
2. Откройте игру в **Firefox** (или другом браузере) - токен должен быть **другой**
3. Откройте игру в **инкогнито** - токен должен быть **другой** (новое устройство)

## Способ 3: Проверка через логи

1. Откройте консоль (F12)
2. Перезагрузите страницу
3. Ищите в логах:
   - `[GAME] 🔑 Используется существующий access_token` - токен НЕ обновляется ✅
   - `[GAME] 🔄 Генерация нового access_token` - токен обновляется ❌

## Способ 4: Сброс и проверка генерации

1. В консоли выполните:
```javascript
resetUserToken()
```

2. Страница перезагрузится
3. В логах должно быть: `[GAME] 🔄 Генерация нового access_token`
4. Запишите новый токен
5. Перезагрузите страницу еще раз
6. Токен должен остаться **таким же**

## Способ 5: Проверка через Network tab

1. Откройте DevTools → Network
2. Перезагрузите страницу
3. Найдите запрос к iframe (game file)
4. Посмотрите параметр `access_token` в URL
5. Перезагрузите еще раз
6. `access_token` в URL должен быть **таким же**

## Ожидаемое поведение:

✅ **Правильно:**
- Токен одинаковый при каждой перезагрузке
- Токен разный в разных браузерах/устройствах
- В логах: "Используется существующий access_token"

❌ **Неправильно:**
- Токен меняется при каждой перезагрузке
- В логах каждый раз: "Генерация нового access_token"

