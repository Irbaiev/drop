# 🚀 Быстрый старт - Анализ запросов stake.com

## ⚠️ Проблема с CSP (Content Security Policy)

Stake.com блокирует загрузку файлов с `localhost:8080` из-за политики безопасности. 

## ✅ Решение: Вставка кода напрямую в консоль

### Способ 1: Компактная версия (рекомендуется)

1. **Откройте страницу игры на stake.com:**
   ```
   https://stake.com/ru/casino/games/mirrorimage-drop-the-boss-trump
   ```

2. **Откройте консоль браузера:**
   - Нажмите `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
   - Перейдите на вкладку **Console**

3. **Скопируйте и вставьте код:**
   - Откройте файл `network-analyzer-inline.js`
   - Скопируйте **весь код** (начиная с `(function() {`)
   - Вставьте в консоль и нажмите `Enter`

4. **Обновите страницу:**
   - Нажмите `F5` или `Ctrl+R`
   - Все запросы будут автоматически перехвачены и залогированы

5. **Анализируйте запросы:**
   - В консоли ищите записи с 🔴 - это важные запросы, связанные с session
   - Используйте команды:
     ```javascript
     // Получить все запросы
     getCapturedRequests()
     
     // Экспортировать в JSON файл
     exportCapturedRequests()
     
     // Очистить список
     clearCapturedRequests()
     ```

### Способ 2: Полная версия (если нужны все функции)

1. Откройте файл `network-analyzer.js` в текстовом редакторе
2. Скопируйте весь код
3. Вставьте в консоль на странице stake.com
4. Нажмите `Enter`

### Способ 3: Через DevTools Snippets (для повторного использования)

1. Откройте DevTools (`F12`)
2. Перейдите на вкладку **Sources** (или **Snippets**)
3. Нажмите `+` для создания нового snippet
4. Вставьте код из `network-analyzer-inline.js`
5. Сохраните (например, как "Network Analyzer")
6. Правой кнопкой → **Run** или нажмите `Ctrl+Enter`

## 📋 Что искать в логах

После активации анализатора и обновления страницы, ищите в консоли:

- 🔴 **IMPORTANT** - запросы, связанные с session/game
- Запросы к доменам: `stake-engine.com`, `rgs.twist-rgs.com`
- POST запросы с параметрами игры
- Запросы, содержащие `sessionID` в URL или теле

### Примеры важных запросов:

```
🔴 POST https://rgs.stake-engine.com/session/start
🔴 GET https://stake-engine.com/api/game/session
🔴 POST https://stake.com/api/casino/session
```

## 🔍 Анализ найденных запросов

Когда найдете запрос, который возвращает `sessionID`:

1. **Кликните на запрос в консоли** (если он развернут)
2. **Скопируйте:**
   - URL запроса
   - Метод (GET/POST)
   - Заголовки (особенно Authorization, если есть)
   - Тело запроса (если POST)
   - Формат ответа

3. **Пример найденного endpoint'а:**
   ```javascript
   // URL: https://rgs.stake-engine.com/session/start
   // Method: POST
   // Headers: { "Content-Type": "application/json" }
   // Body: { "gameId": "mirrorimage-drop-the-boss-trump", "currency": "USD" }
   // Response: { "sessionID": "xxx-xxx-xxx", "rgs_url": "rgs.stake-engine.com" }
   ```

## 💡 Альтернатива: Bookmarklet для быстрого извлечения sessionID

Если вам не нужно анализировать все запросы, а просто нужно получить sessionID:

1. Откройте файл `stake-session-grabber.js`
2. Скопируйте код (начиная с `javascript:`)
3. Создайте новую закладку в браузере
4. Вставьте код в поле URL закладки
5. На странице игры нажмите на закладку
6. Вы будете перенаправлены на офлайн-страницу с новым sessionID

## 🎯 Следующие шаги

После того, как найдете API endpoint для получения sessionID:

1. Можно создать функцию для автоматического получения sessionID
2. Интегрировать в `index.html` или `runtime/offline.js`
3. Настроить автоматическое обновление sessionID при каждой загрузке страницы

## ❓ Частые проблемы

**Q: Код не выполняется в консоли**
- Убедитесь, что скопировали весь код целиком
- Проверьте, что нет синтаксических ошибок (консоль покажет их)

**Q: Запросы не перехватываются**
- Убедитесь, что обновили страницу после вставки кода
- Проверьте, что код выполнился без ошибок (должно появиться сообщение "✅ Анализатор активирован!")

**Q: Слишком много запросов в логе**
- Используйте фильтр консоли: введите `🔴` чтобы видеть только важные запросы
- Или используйте `getCapturedRequests()` для получения только важных запросов

